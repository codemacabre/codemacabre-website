---
const { date, description, endDate, title, url } = Astro.props
---
<li>
  <a class="title" href={url}>{title}</a>
  <span class="dates">
    ({new Date(date).getFullYear()}{endDate ? ` - ${new Date(endDate).getFullYear()}` : ''})
  </span>
  <p class="description">{description}</p>
</li>

